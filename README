1. Необходимо в отдельных пакетах реализовать АТД Словарь на массивах используя открытое и закрытое хеширование. Необходимые операции (открытые методы) перечислены ниже.
2. Реализовать класс с методом main(), в котором решается задача:

общество защиты тунцов (ОЗТ) имеет базу данных с записями результатов самого последнего голосования законодателей по законопроектам об охране тунцов. База данных состоит из двух списков (множеств) имен законодателей, которые названы goodguys (хорошие парни) и badguys (плохие парни). Например, после голосования все законодатели, проголосовавшие за законопроект, заносятся в список goodguys и удаляются из списка badguys, тогда как над оппонентами этого законопроекта совершается обратная процедура. Законодатели, не принимавшие участие в голосовании, остаются в тех списках, в которых они были ранее. Для управления описываемой базой данных при вводе имен законодателей применяются односимвольные команды, за символом команды через пробел следуют до 10 символов – имя законодателя. Каждая команда располагается в отдельной строке. Используются следующие односимвольные команды:
F (законодатель голосовал «правильно»);
U (законодатель голосовал «неправильно»);
? (надо определить статус законодателя).
Использовать символ 'Е' без имени для обозначения окончания процесса ввода списка законодателей.

АТД Словарь
A – множество, x – элемент.

Операции (указаны все и явные, и неявные параметры):
MEMBER(А, x) – имеет аргументами множество А и объект х того же типа, что и элементы множества А, и возвращает булево значение true (истина), если х принадлежит А, и значение false (ложь), если х не принадлежит A.
MAKENULL(A) – присваивает множеству А значение пустого множества.
INSERT(А, x) – делает х элементом множества А.
DELETE(А, x) – удаляет элемент х из множества А.

Лабораторная работа состоит из двух этапов: проектирование и реализация. Пока не принят этап проектирования, реализация не рассматривается!
Проект представляется в виде текстового файла, в котором описываются все классы и их данные. Кроме того, на естественном языке перечисляются все действия для всех конструкторов, операций и вспомогательных методов.

Общие требования:
Не использовать коллекции.
Не использовать доступ по умолчанию. Указывать доступ для всего, классов, данных, методов.
Для всех классов перед каждым методом (кроме main()), в комментариях должно быть записано: какую задачу решает метод, какие параметры ему передаются, что возвращается в результате.
Для всех объявленных в методах переменных, включая метод main(), в комментариях необходимо указать их назначение.
Все важные для понимания методов моменты должны сопровождаться комментариями.
